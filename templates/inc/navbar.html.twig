<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    {% if app.request.get('_route')!='home' %}
        <a class="navbar-brand" href="{{path('home')}}">
            {#<img src="{{ asset('assets/img/logo.png') }}" alt="DeadLikeMe" class="img-fluid">#}
            Sortir.com
        </a>
    {% endif %}
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_home') }}">Admin</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('trip_home') }}">Sorties</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('profil') }}">Profil</a>
                    </li>
                </ul>

        <div class="nav navbar-nav ml-auto">

            <p>
                {% if app.user %}
                <span class="btn btn-default text-light">Bonjour {{ app.user.firstname }}</span>
                <a href="{{ path('logout') }}" class="btn btn-sm btn-outline-danger" title="Déconnexion"><i
                            class="fas fa-power-off"></i></a>
            </p>
            {% else %}
                <p class="text-right">
                    <a href="{{ path('login') }}" class="btn btn-sm btn-outline-info" title="Se connecter"><i
                                class="fas fa-power-off"></i></a>
                </p>
            {% endif %}

        </div>
    </div>
</nav>